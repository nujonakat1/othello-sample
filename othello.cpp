#include<stdio.h>
#include<termios.h>

#define BOARD_WIDTH 8
#define BOARD_HEIGHT 8
int main(){
    for(int y=0; x<BOARD_HEIGHT: y++)
        for(int x=0; x<BOARD_WIDTH; x++)
            printf("・");

    // _getch();
    // return 0;
}
